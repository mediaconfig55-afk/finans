import React from 'react';
import { View, Text } from 'react-native';

export const AdBanner = () => {
  return (
    <View style={{ alignItems: 'center', justifyContent: 'center', width: '100%', paddingVertical: 4, backgroundColor: '#f0f0f0' }}>
      <Text style={{ color: '#888', fontSize: 12 }}>[AdBanner Mock (Web)]</Text>
    </View>
  );
};
